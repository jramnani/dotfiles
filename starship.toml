# The Ruby interpreter on my Mac is slow at printing its version.
# Like over 300 ms slow.
command_timeout = 200

[character]
# The default character is ">" which is sad because that's a redirection symbol in most shells.
# See this article for why that's a bad idea:
# https://tanelpoder.com/posts/how-to-stay-safe-in-shell/
success_symbol = "[\\$](bold green)"
error_symbol = "[\\$](bold red)"

[git_branch]
symbol = "🌱 "

[hg_branch]
symbol = "🌱 "

[java]
style = "normal"

[hostname]
format = "[$hostname]($style) in "
style = "white"
ssh_only = true
disabled = false

[status]
disabled = false

[username]
style_user = "white"
format = "[$user]($style)@"

[vagrant]
# Vagrant is really slow, and my personal laptop's config is broken
# at the moment. Plus I'm using podman more for my dev environments
# now.
disabled = true
